- [ ] SSH
- [ ] how to build multiplatform
- [ ] runtime with web
- [ ] ci/cd release
- [ ] how to custom hub
- [ ] in desktop apps, can settings hub its machine self or setting base url for it
- [ ] machine dialog (`--hub-url ${hubUrl} --hub-key <key>`): hub-url should come from tailscale serve, so desktop/hub needs tailscale installed — if not, show install/join UI instead of raw args, so hub and runtime end up on the same tailscale account
- [ ] Build agent can take over tickets
ss