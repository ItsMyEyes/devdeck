#!/bin/sh
# Runs before every commit — mirrors the "Frontend typecheck" step in
# .github/workflows/test.yml so type errors are caught locally, not in CI.
# Enable once per clone: git config core.hooksPath .githooks

set -e

echo "Running frontend typecheck..."
cd "$(git rev-parse --show-toplevel)/frontend" && npm run typecheck
